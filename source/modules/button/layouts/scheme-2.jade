block text
block href
block className

if submit
  input( type="submit" value="#{ text }" name="BuyButton" )
else if href
  a( href="#{href}" class="#{ className }" ).b-button.i-scheme-2 #{ text }
else
  button( type="submit" ).b-button.i-scheme-2 #{ text }